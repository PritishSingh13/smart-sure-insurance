server:
  port: 8082

spring:
  application:
    name: policy-service

  datasource:
    url: jdbc:mysql://localhost:3306/smartsure_policy
    username: root
    password: Rsrooms201@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # auto create table
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka